module <%= @options.module_name %>
  class <%= @options.name %>
    include BlockStack::Model::Dynamic(<%= @options.database ? ":#{@options.database}" : '' %>)
    <% @options.fields.each do |field, type| %>
    <%= "attr_#{type} :#{field}" %>
    <% end %>
  end
end
